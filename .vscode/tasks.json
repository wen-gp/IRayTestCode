{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build TbdpApplication",
      "type": "shell",
      "command": "cd ${workspaceFolder}/build && cmake --build .",  // 编译命令
      "group": {
        "kind": "build",
        "isDefault": true 
      },
      "problemMatcher": ["$gcc"]
    }
  ]
}